var s = 100;
var p = "software engineer";
var c = "companyA";
var years = 2;
Person
class Person
{
  var salary;
  var position;
  var company;
  var yearsInPos;
  constructor()
  {
    salary = salary;
    //need to concatenate the values in that field into the three different instance variables
    //so that means I need to get access to the notes data.  we also need to determine how they should format it
    //so i gotta refer to the "reading the note field of a transaction"
  }
  personSalary()
  {
    return salary;
  }
  personPosition()
  {
    return position;
  }
  personCompany()
  {
    return company;
  }
}
